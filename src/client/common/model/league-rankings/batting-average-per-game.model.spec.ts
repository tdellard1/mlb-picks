import { BattingAveragePerGame } from './batting-average-per-game.model';

describe('BattingAveragePerGame', () => {
  it('should create an instance', () => {
    expect(new BattingAveragePerGame()).toBeTruthy();
  });
});
