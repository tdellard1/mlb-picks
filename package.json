{
  "name": "mlb-picks",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf src/server/dist",
    "build": "npm run build:client && npm run build:server",
    "start": "cd src/server && npm run start",
    "restart": "npm run clean && npm run build && npm run start",
    "build:client": "cd src/client && npm run build",
    "build:server": "cd src/server && npm run build",
    "install": "npm run install:client && npm run install:server",
    "install:server": "cd src/client && npm run install",
    "install:client": "cd src/server && npm run install",
    "heroku-postbuild": "npm install -g && npm run install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tdellard1/mlb-picks.git"
  },
  "private": true,
  "devDependencies": {
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": "20.13.1",
    "npm": "10.5.2"
  }
}
