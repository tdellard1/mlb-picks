import { AverageRunsPerGame } from './average-runs-per-game';

describe('AverageRunsPerGame', () => {
  it('should create an instance', () => {
    expect(new AverageRunsPerGame()).toBeTruthy();
  });
});
